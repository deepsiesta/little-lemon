import React from "react";
import headerLogo from "../assets/header_logo.png"

const Header = () => {
    return (
        <header>
            <img src={headerLogo} />
        </header>
    )
}

export default Header;
